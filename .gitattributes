* text=auto eol=lf
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.png binary
*.zip binary
# Copie à l'identique de la source (empreinte SHA-256 vérifiée par les tests) : git n'y touche pas.
data/wada.json -text
